Loglook is a python script that analyzes the maillog log (/var/log/maillog) file sorting the number of log entries by the IP address of the person attempting to send mail to the server.  It was created to identify the most frequent spammers on my network.  It's a quick project that will probably be done in a day or so....